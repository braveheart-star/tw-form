import { TwLabelDirective } from './tw-label.directive';

describe('TwLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new TwLabelDirective();
    expect(directive).toBeTruthy();
  });
});
